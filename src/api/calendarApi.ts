// import axios from 'axios';

// const API_BASE_URL = 'http://localhost:5000/api'; // backend base

// export const fetchActivities = async (week: string) => {
//   const res = await axios.get(`${API_BASE_URL}/activities/${week}`);
//   return res.data;
// };

// export const createActivity = async (activity: {
//   day: string;
//   time: string;
//   className: string;
//   room: string;
//   resource: string;
//   week: string;
// }) => {
//   const res = await axios.post(`${API_BASE_URL}/activities`, activity);
//   return res.data;
// };

// export const fetchRoomCounters = async () => {
//   const res = await axios.get(`${API_BASE_URL}/room-counters`);
//   return res.data;
// };
